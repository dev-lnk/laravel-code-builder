<?php

declare(strict_types=1);

namespace {namespace};

use {model_namespace};

final class Add{name}
{
    public function handle(array $data): {name}
    {
        $model = new {name}();

        $model->fill($data);

        $model->save();

        $model->refresh();

        return $model;
    }
}