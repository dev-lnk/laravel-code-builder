<?php

declare(strict_types=1);

namespace {namespace};

use {model_namespace};

final class {name}
{
    public function handle(array $data): {model_name}
    {
        $model = new {model_name}();

        $model->fill($data);

        $model->save();

        $model->refresh();

        return $model;
    }
}